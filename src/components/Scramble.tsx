export default function Scramble(props: {className?: string}) {
    const { className } = props;

    return (
        <div className={"text-center p-4 " + className} >
            Test Scramble
        </div>
    )
}
